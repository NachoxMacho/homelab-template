# Cilium

Cilium is a Container Network Interface (CNI), that is needed by Kubernetes to setup networking within the cluster.

## Prerequisites

- A deployed Kubernetes cluster (For an example see the [Talos README](../talos/README.md)
- Helm CLI Installated:

## Installation

This will cover installing the Cilium CNI through helm. For more information refer to [Cilium's Documentation]().


